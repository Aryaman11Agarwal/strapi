This folder contains a minimal admin UI skeleton for the audit-logs plugin.

It intentionally contains a very small entrypoint to avoid adding build complexity in the monorepo.

You can extend this folder with React pages and components to surface audit logs in the Strapi admin.
